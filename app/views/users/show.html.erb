<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
</h1>

  <div class="container">
    <div class="table-responsive">
      <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th><%= @user.email %></th>
            <th><%= @user.area %></th>
          </tr>
        </thead>
      </table>
    </div>
  </div>